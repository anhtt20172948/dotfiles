# Hosts searched by the `show` and `download` scripts.
#
# One host per line. Blank lines and '#' comments are ignored.
# Names must be resolvable by ssh — normally a Host entry in ~/.ssh/config.
#
# This file exists so the list lives in ONE place: show and download each used
# to carry their own hardcoded copy of it.

jupyter
rack
strong
jetson50
jetson51
jetson52
jetson53
jetson54

# Also defined in ~/.ssh/config but not searched by default — every host listed
# here costs one parallel ssh probe per lookup. Uncomment what you need.
#sirius
#sakura
#spark
#thor_vn
#thor_safie
#thor_mct
