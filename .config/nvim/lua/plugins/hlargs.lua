return {
    'm-demare/hlargs.nvim',
    config = function()
        require('hlargs').setup {
            -- Your setup opts here (leave empty to use defaults)
            -- Example: highlight = { enable = true }
        }
    end,
}
